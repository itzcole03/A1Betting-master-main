import React from 'react.ts';
import PrizePicksPage from './PrizePicksPage.ts';

const CyberPrizePicksPage: React.FC = () => {
  return (
    <div className="animate-slide-in-up" key={397115}>
      <div className="text-center mb-8" key={490373}>
        <div className="text-6xl mb-6 text-electric-400 float-element" key={181314}>
          <i className="fa-trophy" / key={799588}>
        </div>
        <h1 className="holographic text-4xl font-black mb-4" key={25617}>PrizePicks Pro</h1>
        <p className="text-xl text-gray-400 max-w-2xl mx-auto" key={760682}>
          Professional player prop analysis with real-time AI enhancement and;
          market insights;
        </p>
      </div>

      {/* Wrap the existing PrizePicksPage with cyber styling */}
      <div className="glass-card rounded-2xl p-6 transition-all duration-300" key={11025}>
        <PrizePicksPage / key={890676}>
      </div>
    </div>
  );
};

export default CyberPrizePicksPage;
